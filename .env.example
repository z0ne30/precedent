### Database Configuration ###
# Required for Prisma - PostgreSQL connection string
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

### NextAuth Configuration ###
# Obtain these from your Google Cloud Console project (APIs & Services > Credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Generate a strong secret using: openssl rand -base64 32
NEXTAUTH_SECRET=
# Optional: Set NEXTAUTH_URL for production/preview deployments
# NEXTAUTH_URL=https://yourdomain.com

### Development Settings ###
# Set to true to bypass authentication checks in development mode (DO NOT SET IN PRODUCTION)
# NEXT_PUBLIC_SKIP_AUTH_IN_DEV=true
# SKIP_AUTH_IN_DEV=true

### Contact Form Emails (Resend) ###
# Used in app/api/contact/route.ts
RESEND_API_KEY= # Get from Resend dashboard
CONTACT_SENDER_NAME="Your Name or Site Name"
CONTACT_SENDER_EMAIL="your-verified-sender@example.com" # Must be verified with Resend
CONTACT_NOTIFICATION_EMAIL="your-notification-recipient@example.com"
